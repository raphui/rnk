menu "Loader configuration"

choice
    depends on !STATIC_APPS
    prompt "Application loader"

    config ELF_LOADER
        bool "Use ELF loader"

    config RFLAT_LOADER
        bool "Use RFLAT loader"
endchoice

endmenu
